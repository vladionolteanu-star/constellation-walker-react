import { Marker } from 'react-map-gl'
import { motion } from 'framer-motion'

interface EchoMarkerProps {
  echo: any // Will be properly typed later
}

export default function EchoMarker({ echo }: EchoMarkerProps) {
  // Placeholder for now - echoes will be implemented later
  return null
}
